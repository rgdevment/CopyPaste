name: Release v2 (Linux)

on:
    workflow_call:
        inputs:
            version:
                required: true
                type: string
    workflow_dispatch:
        inputs:
            version:
                description: "Version to use (e.g. 2.1.0). Defaults to 2.0.0-dev"
                required: false
                default: "2.0.0-dev"

permissions:
    contents: read

jobs:
    build-linux:
        runs-on: ubuntu-22.04
        timeout-minutes: 45
        name: Build Linux (AppImage, deb, rpm)

        steps:
            - uses: actions/checkout@v6
              with:
                  ref: ${{ github.ref_name }}

            - name: Resolve version
              id: get_version
              run: |
                  VERSION="${{ inputs.version }}"

                  IS_PRERELEASE="false"
                  if [[ "$VERSION" == *-* ]]; then
                    IS_PRERELEASE="true"
                  fi

                  echo "VERSION=$VERSION" >> "$GITHUB_OUTPUT"
                  echo "IS_PRERELEASE=$IS_PRERELEASE" >> "$GITHUB_OUTPUT"
                  echo "Version: $VERSION  PreRelease: $IS_PRERELEASE"

            - uses: subosito/flutter-action@v2
              with:
                  channel: stable
                  cache: true

            - name: Cache pub dependencies
              uses: actions/cache@v5
              with:
                  path: ~/.pub-cache
                  key: ${{ runner.os }}-pub-${{ hashFiles('**/pubspec.yaml') }}
                  restore-keys: ${{ runner.os }}-pub-

            - name: Install Linux build dependencies
              run: |
                  sudo apt-get update
                  sudo apt-get install -y \
                    clang \
                    cmake \
                    desktop-file-utils \
                    libayatana-appindicator3-dev \
                    libfuse2 \
                    libgtk-3-dev \
                    libkeybinder-3.0-dev \
                    liblzma-dev \
                    libx11-dev \
                    libxtst-dev \
                    lld-14 \
                    ninja-build \
                    patchelf \
                    pkg-config \
                    rpm

            - name: Verify Linux toolchain preflight
              run: |
                  set -euo pipefail
                  test -x /usr/lib/llvm-14/bin/ld.lld || test -x /usr/bin/ld.lld-14 || test -x /usr/bin/ld.lld
                  pkg-config --modversion gtk+-3.0
                  pkg-config --modversion keybinder-3.0
                  pkg-config --modversion ayatana-appindicator3-0.1
                  pkg-config --modversion x11
                  pkg-config --modversion xtst

            - name: Install Fastforge
              run: dart pub global activate fastforge

            - name: Install appimagetool
              run: |
                  wget -qO /usr/local/bin/appimagetool \
                    "https://github.com/AppImage/AppImageKit/releases/download/continuous/appimagetool-x86_64.AppImage"
                  chmod +x /usr/local/bin/appimagetool

            - name: Get dependencies
              run: |
                  cd v2/core && flutter pub get
                  cd ../listener && flutter pub get
                  cd ../app && flutter pub get

            - name: Update pubspec version from tag
              run: |
                  VERSION="${{ steps.get_version.outputs.VERSION }}"
                  sed -i "s/^version:.*/version: $VERSION/" v2/app/pubspec.yaml
                  echo "Updated pubspec.yaml version to: $VERSION"

            - name: Package AppImage
              env:
                  APPIMAGE_EXTRACT_AND_RUN: "1"
              run: |
                  cd v2/app
                  fastforge package \
                    --platform linux \
                    --targets appimage \
                    --build-dart-define "APP_VERSION=${{ steps.get_version.outputs.VERSION }}"

            - name: Rename AppImage
              run: |
                  VERSION="${{ steps.get_version.outputs.VERSION }}"
                  APPIMAGE=$(find v2/app/dist v2/app/build -type f -name "*.AppImage" 2>/dev/null | head -n 1)
                  if [[ -z "$APPIMAGE" ]]; then
                    echo "::error::No AppImage generated"
                    exit 1
                  fi
                  mkdir -p v2/app/dist
                  DEST="v2/app/dist/CopyPaste_${VERSION}_x86_64.AppImage"
                  mv "$APPIMAGE" "$DEST"
                  chmod +x "$DEST"
                  echo "Renamed to: $(basename "$DEST")"

            - name: Package deb
              run: |
                  cd v2/app
                  fastforge package \
                    --platform linux \
                    --targets deb \
                    --build-dart-define "APP_VERSION=${{ steps.get_version.outputs.VERSION }}" \
                    --build-skip

            - name: Rename deb
              run: |
                  VERSION="${{ steps.get_version.outputs.VERSION }}"
                  DEB=$(find v2/app/dist v2/app/build -type f -name "*.deb" 2>/dev/null | head -n 1)
                  if [[ -z "$DEB" ]]; then
                    echo "::error::No deb package generated"
                    exit 1
                  fi
                  mkdir -p v2/app/dist
                  DEST="v2/app/dist/CopyPaste_${VERSION}_amd64.deb"
                  mv "$DEB" "$DEST"
                  echo "Renamed to: $(basename "$DEST")"

            - name: Package rpm
              run: |
                  cd v2/app
                  fastforge package \
                    --platform linux \
                    --targets rpm \
                    --build-dart-define "APP_VERSION=${{ steps.get_version.outputs.VERSION }}" \
                    --build-skip

            - name: Rename rpm
              run: |
                  VERSION="${{ steps.get_version.outputs.VERSION }}"
                  RPM=$(find v2/app/dist v2/app/build -type f -name "*.rpm" 2>/dev/null | head -n 1)
                  if [[ -z "$RPM" ]]; then
                    echo "::error::No rpm package generated"
                    exit 1
                  fi
                  mkdir -p v2/app/dist
                  DEST="v2/app/dist/CopyPaste_${VERSION}_x86_64.rpm"
                  mv "$RPM" "$DEST"
                  echo "Renamed to: $(basename "$DEST")"

            - name: Upload artifact
              uses: actions/upload-artifact@v6
              with:
                  name: release-linux
                  path: |
                      v2/app/dist/*.AppImage
                      v2/app/dist/*.deb
                      v2/app/dist/*.rpm
                  retention-days: 5
